import { units } from './units'
import { IBorder } from '../../../types'

const borders: IBorder = {
  radius: {
    base: `3${units}`,
    md: `6${units}`,
    lg: `9${units}`,
    xl: `100${units}`,
  },
}

export default borders
