import { units } from './units'
import { ISpacing } from '../../../types'

const spacing: ISpacing = {
  xxs: `2${units}`,
  xs: `4${units}`,
  sm: `6${units}`,
  default: `8${units}`,
  md: `16${units}`,
  lg: `32${units}`,
  xl: `64${units}`,
  xxl: `128${units}`,
}

export default spacing
